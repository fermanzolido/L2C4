/bin/
build/
.gradle/
.idea/
.DS_Store
nbproject/
*.class

# Eclipse project metadata: local to whoever opened the project, not to the project.
.settings/
.classpath
.project

# Agent and bot instruction folders.
.agents/
.jules/

# Working notes and build scratch, kept out of the tree that ships.
AUDIT-PROGRESS.md
*-DECISIONS.md
ELO_ANALYSIS.md
build_errors.txt
output.txt

# Built by the build, never committed: a stale copy silently shadows the source.
dist/libs/GameServer.jar
dist/libs/LoginServer.jar
